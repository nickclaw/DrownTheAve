var sass = require('node-sass-middleware');

module.exports = function(options) {

    var middleware = sass(options);

    return function *(next) {
        yield middleware.bind(middleware, this.req, this.res);
        yield next;
    }
}
